{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Opinion</h1>
    <table class="table table-hover mt-5">
        <thead>
            <tr>
                <th>id</th>
                <th>Titre</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for opinion in opinions %}
        <tr class="table">
            <td>{{ opinion.id }}</td>
            <td>{{ opinion.title }}</td>
            <td>
                <a href="/opinion/show/{{ opinion.id }}" class="btn btn-primary">Montrer</a>
                <a href="/opinion/edit/{{ opinion.id }}" class="btn btn-warning">Editer</a>
            </td>
        </tr>    
        {% else %}
        <tr>
            <td>Rien à afficher</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="/opinion/add" class="btn btn-primary">Ajouter un avis</a>
{% endblock %}

